# Fresh Redeploy — 2026-06-10 (Branch C: lost-key recovery)

**Why:** Access to the 2026-06-06 public-testnet governance set was lost — the Gov/PG Safe
signer keys, the launch-pack deployer `0x5Af7…7b13`, the original deployer `0xe8E5…754b`,
and the token-minter/faucet EOA `0xA400…64E4` are all unrecoverable. Since `0xA400…64E4`
was the sole minter of all 7 tokens, the old token set's mint authority is permanently
frozen (faucet dead, no bootstrap seed possible). Decision: full fresh redeploy with fresh
governance **and fresh tokens** (Branch C(b)) per
`docs/superpowers/plans/2026-06-09-fresh-key-ceremony-redeploy.md`.

**Code changes enabling this rollout** (branch `ops/2026-06-09-fresh-redeploy`):
- `625a70e` — env-parameterized token addresses (`resolvedTokens()`, `TOKEN_<SYM>` overrides)
  + `DeployTokensFresh.s.sol`; orchestrator's audited oracle-wiring untouched (additive only).
- `e0ef7c2` — order-independent token-param test (repo `vm.setEnv` convention).
- `3efea57` — SDK/app/faucet/memory repointed at the new ledger (SDK 97/97, app 41/41).
- `77eec72` — keeper TRYC peg anchor 0.030 → 0.023 (live TRY/USD drifted past the 1500bps gate).

## Key ceremony (2026-06-10)

All keys freshly generated by the key-holder; private keys never entered the repo/chat.
Gov Safe 3/5 owners (5), PG Safe 2/3 owners (= first 3 Gov owners — 5-person team,
intentional), keeper primary/secondary, treasury, faucet, deployer. Full map:
`arcora-key-ceremony-launch-pack/06-address-map.md`. Deployer key lives in the encrypted
Foundry keystore `arcora-deployer`; keeper keys in VPS Vault `kv/arcora/keeper-arcoradex`
(H-2 split fields); faucet key destined for Vercel `FAUCET_PRIVATE_KEY`.

## Deployed (chainId 5042002, deployer `0xC49303Bda108dE3FFDBDEC069B795b645168D7e6`)

Authoritative ledger: `arcora-key-ceremony-launch-pack/07-deployed-addresses.env`.

| Contract | Address |
|---|---|
| Pool | `0x214825E3e24a07cd58e48267e492320dAccCe2f6` |
| Registry | `0x372f83a1432Aa43b72eDCE083DC8352d9Bfb47f1` |
| LP | `0xc07e979B5Ee023Ad96E65E733b99902306CAFEf2` |
| Guard | `0x06f4EbCd1780721ab2eCDd776294699d2697761d` |
| Gov Safe (3/5) | `0x396145BAB316d84F958368d93ec8984559f0261B` |
| Pause Guardian (2/3) | `0x1098e88b8b243451109D7eA7690f9e2ca7b18280` |
| Timelock | `0xEb2E77144F5BcB854ED75C687988c4F19100e5D7` (deployed delay 0 → raise to 48h via runbook) |

Plus 7 fresh tokens, 7 bounded primaries, 7 secondaries, 7 V1 + 7 V2 aggregators (ledger).

**Sequence executed:** `DeployTokensFresh` (7 tokens + seed mint) → orchestrator dry-run
(all invariants ok) → orchestrator broadcast (FRESH governance mode; GAP#5/H-2/M-1 asserts
green; NAV 648.28 USD) → smoke swap 10 USDC → 9.995 USDT (0.05% fee exact) →
`TransferTokenOwnershipToFaucet` (7 tokens → faucet EOA, registry-driven so no code change)
→ deployer-side governance handoff: Pool/Registry `transferOwnership` → Timelock (pending),
7 primary feeds → Gov Safe (pending).

## Keeper (VPS 194.163.136.1)

- Vault AppRole `keeper-feeds` secret_id had expired (services down since ~Jun 8) — minted
  fresh secret_id, creds reinstalled, login verified.
- Keeper keys written to Vault v2 as `KEEPER_PRIMARY_KEY`/`KEEPER_SECONDARY_KEY` via the
  validated `/root/set-keeper-keys.sh` (derives + matches addresses before writing).
- VPS was running pre-H-2 `fetch-keeper-secret.sh` + single-writer `multi-feed-push.mjs`;
  updated to repo versions (dual-writer), unit gained `TimeoutStartSec=120`, missing
  `arcoradex-guard-monitor.{service,timer}` installed + enabled.
- `.env` rewritten to the new registry/guard/feed addresses
  (backup kept). `FX_REFERENCE_SOURCE=none` (exchangerate.host now keyless-unusable;
  band + peg-drift guards remain active).
- First healthy run: 8 USD-leg feeds updated (both writers, H-2 verified by writer asserts);
  EURC/TRYC/BRLC converging to live FX (capped @150bps per tick by design; service exits 1
  while both legs cap — alert semantics, self-clears at convergence).

## Governance finalization — DONE 2026-06-10

Executed via `contracts/script/ExecuteGovernanceAccepts.s.sol` (Arc has no hosted Safe UI;
signatures produced locally over the safeTxHash, deployer EOA broadcast). All four phases
landed on-chain and were independently re-verified:

- 15 oracle-layer accepts (guard + 7 primaries + 7 secondaries) → owner == Gov Safe.
- Pool + Registry `acceptOwnership` via Timelock batch → owner == Timelock.
- **Pause drill passed:** PG Safe (2/3) paused the Pool; Gov Safe unpaused it through the
  Timelock during the delay-0 window. (`pool.pauseGuardian` was unwired at deploy —
  `address(0)` — and set to the PG Safe by the deployer beforehand; orchestrator gap noted.)
- `Timelock.updateDelay(172800)` — every governance op now waits 48 h.

The deployer EOA retains **no protocol authority** (tokens → faucet, Pool/Registry →
Timelock, oracle layer → Gov Safe). Gov owner keys were used transiently and destroyed
after execution.

**dApp:** production deploy live at swap.arcorapay.xyz with the fresh `FAUCET_PRIVATE_KEY`
(verified deriving to `0xb5f3…08B6`) — pool stats and live transactions read from the new
Pool; faucet UI lists the 7 fresh tokens.

## Outstanding (optional)

1. Legacy `arcora-oracle-refresh.timer` on the VPS still refreshes the long-abandoned v0.7
   feeds from `/root/arcora-ops` — candidate for disablement (operator decision).
2. V1 aggregators (unused; registry points at V2) have pending Gov Safe ownership — optional
   accept, addresses in `contracts/broadcast/DeployPublicTestnet.s.sol/5042002/run-latest.json`.
3. Merge `ops/2026-06-09-fresh-redeploy` to `main` (PR).

## Superseded

The 2026-06-06 deploy (Pool `0x5325…a3bf`, Registry `0xc6D0…7c97`, old tokens) is abandoned:
still deployer-owned by the lost `0xe8E5…754b`, unpaused, mint-frozen. Do not route anything
at it.
