{
  "name": "arcoradex-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter app dev"
  },
  "engines": { "node": ">=22", "pnpm": ">=10" },
  "pnpm": {
    "overrides": {
      "viem": "2.48.8",
      "wagmi": "2.19.5"
    }
  }
}
